# Web çatısı ve sunucu
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Veri katmanı (PostgreSQL + ORM)
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9

# Doğrulama / ayarlar
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Kimlik doğrulama (JWT + şifre hash)
python-jose[cryptography]==3.3.0
passlib==1.7.4

# Yapay zeka — görsel açıklama (Gemini 1.5 Flash)
google-generativeai==0.8.3

python-dotenv==1.0.1
