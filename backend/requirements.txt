# Web çatısı ve sunucu
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Veri katmanı (ORM). Yerel geliştirmede SQLite yeterlidir.
SQLAlchemy==2.0.35

# Doğrulama / ayarlar
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Kimlik doğrulama (JWT + şifre hash)
python-jose[cryptography]==3.3.0
passlib==1.7.4

python-dotenv==1.0.1

# --- OPSİYONEL (şimdilik gerekmez) ---
# PostgreSQL sürücüsü: yalnızca gerçek PostgreSQL'e bağlanınca gerekir.
# psycopg2-binary==2.9.9
#
# Gerçek Gemini görsel açıklaması için. Boş bırakılırsa sistem mock ile çalışır.
# google-generativeai==0.8.3
#
# Gerçek altyazı (Sevda'nın STT hattı) için. Kurulu değilse mock altyazı döner.
# Ayrıca sistemde FFmpeg gerekir (Mac: brew install ffmpeg).
# openai-whisper==20231117